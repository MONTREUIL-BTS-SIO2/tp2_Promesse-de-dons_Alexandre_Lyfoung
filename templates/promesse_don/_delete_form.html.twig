<form method="post" action="{{ path('app_promesse_don_delete', {'id': promesse_don.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promesse_don.id) }}">
    <button class="btn">Delete</button>
</form>
