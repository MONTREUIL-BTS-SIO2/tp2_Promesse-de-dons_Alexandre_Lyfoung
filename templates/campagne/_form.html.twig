{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-lg btn-success">{{ button_label|default('Save') }}</button>
    {% if id is not null %}
    <a href="{{ path('app_campagne_show', {'id':id}) }}" class="btn btn-secondary" type="button">Retour</a>
    {% endif %}
{{ form_end(form) }}
