{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} - Edition {% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1 class="diplay-1">Edit </h1>
            </div>
        </div>
        <hr/>
        <div class="row">
            {{ include('campagne/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
    <a href="{{ path('app_campagne_index') }}">back to list</a>

    {{ include('campagne/_delete_form.html.twig') }}
{% endblock %}
